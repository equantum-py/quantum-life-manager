import { AppAlert } from '../../types';import { Badge } from '../ui/Badge';import { prettyDate } from '../../utils/dates';
export function AlertCard({alert}:{alert:AppAlert}){const map={red:'border-red-200 bg-red-50',yellow:'border-amber-200 bg-amber-50',blue:'border-blue-200 bg-blue-50',green:'border-green-200 bg-green-50'};return <article className={`rounded-3xl border p-4 ${map[alert.level]}`}><div className="flex justify-between gap-2"><h3 className="font-bold">{alert.title}</h3><Badge tone={alert.level==='red'?'red':alert.level==='yellow'?'yellow':alert.level}>{prettyDate(alert.date)}</Badge></div><p className="mt-1 text-sm text-slate-600">{alert.description}</p><p className="mt-3 text-sm font-semibold text-slate-800">Acción: {alert.action}</p></article>}
