import { Link } from 'react-router-dom';import * as Icons from 'lucide-react';import { Section } from '../../types';
export function SectionCard({section}:{section:Section}){const Icon=(Icons as any)[section.icon]||Icons.Folder;return <Link to={`/sections/${section.id}`} className="block rounded-3xl bg-white p-5 shadow-sm border border-slate-100"><div className="flex items-center gap-3"><span className="rounded-2xl bg-blue-100 p-3 text-blue-700"><Icon size={24}/></span><div><h3 className="font-bold">{section.name}</h3><p className="line-clamp-2 text-sm text-slate-500">{section.description}</p></div></div></Link>}
