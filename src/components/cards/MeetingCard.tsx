import { Meeting } from '../../types';import { Badge } from '../ui/Badge';import { prettyDate } from '../../utils/dates';
export function MeetingCard({meeting,onEdit,onDelete}:{meeting:Meeting;onEdit?:()=>void;onDelete?:()=>void}){return <article className="rounded-3xl bg-white p-4 shadow-sm border border-slate-100"><div className="flex justify-between gap-2"><h3 className="font-bold">{meeting.title}</h3><Badge>{meeting.type}</Badge></div><p className="text-sm text-slate-500">{meeting.description}</p><p className="mt-3 text-sm font-semibold text-slate-700">{prettyDate(meeting.date)} · {meeting.startTime}-{meeting.endTime}</p><p className="text-sm text-slate-500">{meeting.location}</p><div className="mt-3 flex gap-2 text-sm font-semibold"><button onClick={onEdit} className="rounded-xl bg-blue-50 px-3 py-2 text-blue-700">Editar</button><button onClick={onDelete} className="rounded-xl bg-red-50 px-3 py-2 text-red-700">Eliminar</button></div></article>}
