import { Task } from '../../types';import { Badge } from '../ui/Badge';import { prettyDate } from '../../utils/dates';
export function TaskCard({task,onDone,onEdit,onDelete}:{task:Task;onDone?:()=>void;onEdit?:()=>void;onDelete?:()=>void}){const tone=task.priority==='Urgente'?'red':task.priority==='Alta'?'yellow':'blue';return <article className="rounded-3xl bg-white p-4 shadow-sm border border-slate-100"><div className="flex items-start justify-between gap-2"><h3 className="font-bold">{task.title}</h3><Badge tone={tone}>{task.priority}</Badge></div><p className="mt-1 text-sm text-slate-500">{task.description}</p><div className="mt-3 flex flex-wrap gap-2"><Badge tone="slate">{task.status}</Badge><Badge tone="blue">{prettyDate(task.dueDate)}</Badge></div><div className="mt-4 flex gap-2 text-sm font-semibold"><button onClick={onDone} className="rounded-xl bg-green-50 px-3 py-2 text-green-700">Terminar</button><button onClick={onEdit} className="rounded-xl bg-blue-50 px-3 py-2 text-blue-700">Editar</button><button onClick={onDelete} className="rounded-xl bg-red-50 px-3 py-2 text-red-700">Eliminar</button></div></article>}
