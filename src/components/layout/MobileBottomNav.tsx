import { NavLink } from 'react-router-dom';import { Bell, CalendarDays, CheckSquare, Home, Menu } from 'lucide-react';
const items=[['/dashboard',Home,'Inicio'],['/tasks',CheckSquare,'Tareas'],['/agenda',CalendarDays,'Agenda'],['/alerts',Bell,'Alertas'],['/sections',Menu,'Más']] as const;
export function MobileBottomNav(){return <nav className="fixed bottom-0 left-0 right-0 z-40 border-t border-slate-200 bg-white/95 px-2 pb-[env(safe-area-inset-bottom)] shadow-soft md:hidden"><div className="mx-auto grid max-w-md grid-cols-5">{items.map(([to,Icon,label])=><NavLink key={to} to={to} className={({isActive})=>`tap flex flex-col items-center gap-1 rounded-2xl px-2 py-3 text-xs font-semibold ${isActive?'text-blue-600':'text-slate-500'}`}><Icon size={22}/>{label}</NavLink>)}</div></nav>}
