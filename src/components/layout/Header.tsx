import { LogOut } from 'lucide-react';import { authService } from '../../services/authService';
export function Header(){const user=authService.current();return <header className="sticky top-0 z-30 border-b border-slate-200 bg-[#F8FAFC]/95 px-4 py-3 backdrop-blur"><div className="mx-auto flex max-w-5xl items-center justify-between"><div><p className="text-xs text-slate-500">Quantum Life Manager</p><h1 className="text-xl font-bold">Hola, {user?.name.split(' ')[0]}</h1></div><button className="rounded-full bg-white p-3 shadow-sm" onClick={()=>{authService.logout();location.href='/login'}}><LogOut size={20}/></button></div></header>}
