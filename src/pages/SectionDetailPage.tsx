import { useParams } from 'react-router-dom';import { ProjectCard } from '../components/cards/ProjectCard';import { TaskCard } from '../components/cards/TaskCard';import { MeetingCard } from '../components/cards/MeetingCard';import { mockMeetings } from '../data/mockMeetings';import { mockProjects } from '../data/mockProjects';import { mockSections } from '../data/mockSections';import { mockTasks } from '../data/mockTasks';
export function SectionDetailPage(){const {sectionId}=useParams();const section=mockSections.find(s=>s.id===sectionId)!;return <div className="space-y-4"><section className="rounded-[2rem] bg-white p-5 shadow-sm"><h2 className="text-2xl font-extrabold">{section.name}</h2><p className="mt-2 text-slate-500">{section.description}</p></section><h3 className="font-bold">Tareas</h3>{mockTasks.filter(t=>t.sectionId===sectionId).map(t=><TaskCard key={t.id} task={t}/>) }<h3 className="font-bold">Agenda</h3>{mockMeetings.filter(m=>m.sectionId===sectionId).map(m=><MeetingCard key={m.id} meeting={m}/>) }{sectionId==='equantum'&&<><h3 className="font-bold">Proyectos eQuantum</h3>{mockProjects.map(p=><ProjectCard key={p.id} project={p}/>)}</>}</div>}
