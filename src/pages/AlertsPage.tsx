import { AlertCard } from '../components/cards/AlertCard';import { mockProjects } from '../data/mockProjects';import { mockTasks } from '../data/mockTasks';import { mockMeetings } from '../data/mockMeetings';import { authService } from '../services/authService';import { buildAlerts } from '../utils/alerts';
export function AlertsPage(){const user=authService.current()!;const tasks=mockTasks.filter(t=>user.sections.includes(t.sectionId));const meetings=mockMeetings.filter(m=>user.sections.includes(m.sectionId));const projects=user.sections.includes('equantum')?mockProjects:[];const alerts=buildAlerts(tasks,meetings,projects);return <div className="space-y-4"><h2 className="text-2xl font-extrabold">Alertas</h2>{alerts.map(a=><AlertCard key={a.id} alert={a}/>)}</div>}
