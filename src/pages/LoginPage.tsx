import { FormEvent, useState } from 'react';import { Navigate } from 'react-router-dom';import { Button } from '../components/ui/Button';import { Input } from '../components/ui/Input';import { authService } from '../services/authService';
export function LoginPage(){const [email,setEmail]=useState('derlis@quantum.local'),[password,setPassword]=useState('123456'),[error,setError]=useState('');if(authService.current())return <Navigate to="/dashboard"/>;function submit(e:FormEvent){e.preventDefault();try{authService.login(email,password);location.href='/dashboard'}catch(err){setError((err as Error).message)}}return <main className="min-h-screen bg-gradient-to-b from-blue-50 to-slate-50 p-5 flex items-center"><form onSubmit={submit} className="mx-auto w-full max-w-md rounded-[2rem] bg-white p-6 shadow-soft"><p className="text-sm font-bold text-blue-600">Privado · Mobile-first</p><h1 className="mt-2 text-3xl font-extrabold">Quantum Life Manager</h1><p className="mt-2 text-slate-500">Organiza familia, iglesia, trabajo, estudios y eQuantum desde un solo lugar.</p><div className="mt-6 space-y-3"><Input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><Input value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" type="password"/>{error&&<p className="text-sm text-red-600">{error}</p>}<Button className="w-full">Entrar</Button></div><div className="mt-5 rounded-2xl bg-slate-50 p-4 text-xs text-slate-600"><b>Credenciales:</b><br/>derlis@quantum.local · daniel@quantum.local · gabriela@quantum.local<br/>Password: 123456</div></form></main>}
